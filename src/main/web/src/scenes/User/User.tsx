import React from "react";

import {UserModel} from "./UserModel";


export const UserDetail: React.FC<UserModel> = (props: UserModel) => {
    const {
       /* accountId,
        image,
        course,
        followers,
        username,*/

    } = props;
   // return UserDetail(accountId,image,course,followers,username)

    return null;
}
